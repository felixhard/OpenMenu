import Foundation

/// Namespace for shared OpenMenu metadata.
public enum OpenMenu {
    /// Reverse-DNS bundle identifier prefix used across features.
    public static let bundleIDPrefix = "software.openmenu"
}
